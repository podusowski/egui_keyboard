[package]
name = "demo_native"
version.workspace = true
edition = "2021"
publish = false
default-run = "demo_native"

[dependencies]
demo = { path = "../demo" }
eframe = { version = "0.30", features = ["wgpu", "android-game-activity"] }
env_logger = "0.11"
